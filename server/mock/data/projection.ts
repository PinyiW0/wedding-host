// 投影設定 mock 資料（空 seed：未設定時 GET 回預設值）

import type { ProjectionSettings } from '../../../app/types/api/projection'

export type MockProjectionSettings = ProjectionSettings

export const mockProjectionSettings: MockProjectionSettings[] = []
